<Window x:Class="Get_TLE.Views.AddMappinngWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Get_TLE.Views"
        mc:Ignorable="d"
        Title="Новая запись" Height="300" Width="300" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- NORAD ID -->
        <TextBlock Text="NORAD ID:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="NoradTextBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Название -->
        <TextBlock Text="Название:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="NameTextBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- FakeID -->
        <CheckBox x:Name="FakeCheckBox" Content="FakeID" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2"
                  Margin="5" Checked="FakeCheckBox_Checked" Unchecked="FakeCheckBox_Unchecked"/>

        <!-- Панель FakeID -->
        <StackPanel x:Name="FakePanel" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Visibility="Collapsed" Margin="5">
            <TextBlock Text="Новый ID:"/>
            <TextBox x:Name="FakeIdTextBox" Margin="0,0,0,5"/>
            <TextBlock Text="Новое имя:"/>
            <TextBox x:Name="FakeNameTextBox"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="OK" Width="80" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Отмена" Width="80" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
